spring:
  application:
    name: database

  datasource:
    url: jdbc:mysql://localhost:3306/fastcampusdb
    username: greenlemonT
    password: greenlemonT4951!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
